import { StyleSheet } from "react-native";

export const userMenu = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  button: {
    marginVertical: 8,
  },
});
